#include <iostream>
using namespace std;

int main() {
  string input = "";
  cin >> input;
  // parse input into a vector(array has to predefine its size, so it's not proper to use in this case because we don't know the size of options yet)
  // * array, vector, list
  // get the size of vector,
  // draw a number from 0 to the size of vector, 
  // use that number as an index and choose value from vector
  // print or return the selected value
  cout << input;
  return 0;
}